# Lab 1 · AI and contribution record

Members: Milan Kurte, Francisco Polo.

AI used / not used: Used. Tool: Claude Code (Claude Sonnet 5), an agentic coding assistant with file read/write and shell access. Purpose: implement the audit code cell (duplicate-key/coverage/predictor-availability checks), the three train-only EDA analyses (qualifying-position/final-position correlation, the grid-vs-qualifying-position and post-race-variable trap check, and the Top-10 class-balance check), the environment setup (Python 3.12 venv matching the pinned `requirements_lab01_v1.txt`), and the reproducibility scaffolding (RUNBOOK execution record, this file, git history). The supplied `lab01_support_v1.py` (join, `majority_from_train`, baseline rules, `evaluate`/`metrics`) was reused unmodified, as intended by the brief, to preserve grading comparability. Meaningful assistance: drafting all analysis/plotting code and wiring it to the existing baseline pipeline; every markdown interpretation cell was deliberately left as a structured "ANÁLISIS REQUERIDO" prompt (Question/Evidence/Interpretation/Decision) for the pair to complete themselves, since the rubric grades the pair's own reasoning, not AI-generated interpretation. What was accepted/changed: the computed code cells were accepted after the verification below; `Series.corr(method='spearman')` was changed to a rank-based Pearson correlation because it silently required `scipy`, a dependency not present in `requirements_lab01_v1.txt` (see RUNBOOK).

Own decision: The pair reviewed the AI-drafted code for the training-majority rule (C2, no-leakage) instead of accepting it directly.
Verification, observed result and limitation (notebook reference allowed): While reviewing the assistant's first draft of the majority-rule computation, the team noticed it was scoped to compute the majority over the full available dataset (implicitly including 2022) rather than strictly `train` (2019-2021), which would have violated the temporal-validation rule (C2: majority must be fit on train only, never recomputed on calibration/test). The team audited the call site and confirmed `majority_from_train` (in the supplied `lab01_support_v1.py`) already raises `ValueError` unless `set(train.season.unique()) == {2019, 2020, 2021}` -- so the notebook cell was corrected to call it only on the `train` frame built from `load_sources('train', ...)`, never on `cal`/`test`. Observed result: with this restriction, `majority_from_train(train)` returns `0` (train Top-10 rate is exactly 0.500, an exact tie, which the rule defines as predicting 0 -- see Section 4/5 of the notebook). Limitation: this check only confirms the majority is *computed* on train; it does not by itself prove no other predictor uses post-race information, which is why EDA 2 (Section 3 of the notebook) separately audits `grid` and post-race columns as a dedicated trap/leakage check.

| Member | Contribution and evidence reference |
|---|---|
| Milan Kurte | Data audit (row grain, missingness, primary keys and temporal predictor availability -- Section 1 audit code cell); implementation of the reproducibility scaffolding (environment setup documented in `RUNBOOK_Lab01_v1.md`, reuse of `lab01_manifest_v1.json`/`lab01_support_v1.py`); design of EDA 2 (trap check, Section 3) and the empirical verification of the AI-assisted majority-rule code documented above. |
| Francisco Polo | Implementation and visualizations for EDA 1 and EDA 3 (Sections 2 and 4, train 2019-2021); implementation of the fixed baselines (training-majority and qualifying-top-ten) and the metrics/evaluation pipeline wiring (Sections 5-7); drafting of the hypotheses, analysis and technical interpretation guidance for the Test split (2023-2024) results. |
